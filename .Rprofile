

if(interactive()){
    library(colorout) # Uncomment if R running in a Tmux pane
    # library(setwidth) # Uncomment if R running in a Tmux pane
    options(nvimcom.verbose = 1) # To know nvimcom was successfully loaded
    library(nvimcom)
}
